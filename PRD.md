# PRD — 반도체 시료 생산주문관리 프로그램

## 1. 개요

반도체 시료(Sample)의 주문 접수부터 생산, 출고까지 전체 흐름을 관리하는
Python 콘솔(CLI) 기반 프로그램. 도메인 규칙과 메뉴 구성의 상세 정의는
[CLAUDE.md](./CLAUDE.md)를 따른다.

## 2. 목적

- 고객 주문을 접수하고, 재고 상황에 따라 자동으로 승인/생산 분기 처리
- 재고 부족 시 생산 라인에서 수율/오차를 고려한 생산량을 계산해 자동 생산
- 담당자가 주문·재고·생산 현황을 한눈에 파악(모니터링)할 수 있도록 지원

## 3. 대상 사용자 (Actors)

| 사용자 | 역할 |
|---|---|
| 고객 | 시료를 지정해 주문(예약) 요청 |
| 주문 담당자 | 접수된 주문을 확인하고 승인/거절 처리 |
| 생산 라인 담당자 | 생산 현황 및 대기열(큐) 확인 |
| 출고 담당자 | CONFIRMED 주문에 대해 출고 처리 |

프로그램은 콘솔 기반 단일 프로그램이며, 위 역할 구분은 논리적 구분으로
별도의 로그인/권한 시스템은 요구되지 않는다.

## 4. 용어 정의

| 용어 | 정의 |
|---|---|
| 시료 (Sample) | 주문 가능한 최소 단위. 시료 ID, 이름, 평균 생산시간, 수율을 가짐 |
| 수율 | 정상 생산품 수 / 총 생산 수 (ex. 0.9) |
| 재고 | 현재 시스템에 확보된 시료의 수량 |
| 생산 큐 | 생산 대기 중인 작업 목록 (FIFO) |

## 5. 기능 요구사항

### 5.1 시료 관리

- **시료 등록**: 시료 ID, 이름, 평균 생산시간, 수율을 입력해 신규 시료를 추가한다.
- **시료 조회**: 등록된 모든 시료 목록과 각 시료의 현재 재고 수량을 표시한다.
- **시료 검색**: 이름 등 속성으로 특정 시료를 검색한다.

### 5.2 주문 (접수 / 승인 / 거절)

- **시료 예약(접수)**: 시료 ID, 고객명, 주문 수량을 입력받아 주문을 생성한다.
  생성된 주문은 `RESERVED` 상태를 가진다.
- **접수된 주문 목록**: `RESERVED` 상태인 주문 목록을 표시한다.
- **주문 승인**: 특정 `RESERVED` 주문을 승인한다.
  - 재고가 주문 수량 이상이면 즉시 `CONFIRMED`로 전환한다.
  - 재고가 부족하면 생산 라인에 해당 주문을 자동 등록하고 `PRODUCING`으로 전환한다.
- **주문 거절**: 특정 `RESERVED` 주문을 거절한다. 즉시 `REJECTED`로 전환하며,
  `REJECTED`는 이후 어떤 화면/집계에서도 유효한 주문으로 취급되지 않는다.

### 5.3 모니터링

- **주문량 확인**: `RESERVED` / `CONFIRMED` / `PRODUCING` / `RELEASE` 상태별
  주문 목록 및 건수를 확인한다. `REJECTED`는 무효 주문이므로 집계에서 제외한다.
- **재고량 확인**: 시료별 현재 재고 수량을 확인하고, 해당 시료에 대한 주문
  수량 대비 재고 상태를 아래 3단계로 표기한다.
  - **여유**: 주문 대비 재고 충분
  - **부족**: 주문 대비 재고 수량 부족
  - **고갈**: 재고 수량이 0

### 5.4 출고 처리

- 재고가 확보되어 `CONFIRMED` 상태인 주문 중 특정 주문을 선택해 출고를 실행한다.
- 실행 시 해당 주문 상태를 `RELEASE`로 전환한다.

### 5.5 생산 라인

- **생산 처리**: 주문 승인 시 재고 부족분에 대해 수율과 오차를 고려해 실제
  생산량과 총 생산 시간을 계산한다.
  - 실 생산량 = `ceil(부족분 / 수율)`
  - 총 생산 시간 = `평균 생산시간 * 실 생산량`
- **생산 라인 동시성**: 생산 라인은 한 번에 하나의 작업만 처리하는 직렬
  구조다. 다음 작업은 현재 작업 완료 후 큐(FIFO)에서 꺼내 시작한다.
- **완료 처리(조회 시점 판단)**: 타이머/스케줄러는 사용하지 않는다.
  콘솔에 어떤 조작이 들어올 때마다 그 시점의 현재 시각을 기준으로 생산
  완료 여부를 판단하고 결과를 반영/출력한다.
  - 진행 중인 작업의 완료 예정 시각이 지났으면 재고를 실 생산량만큼
    증가시키고, 해당 주문 상태를 `PRODUCING` → `CONFIRMED`로 전환한다.
  - 큐에 대기 중인 다음 작업이 있으면 그 시점부터 이어서 시작한다.
  - 이 동기화는 재고/생산 현황과 관련된 메뉴 진입 시 공통으로 수행한다.
- **생산 현황 표기**: 현재 생산 중인 시료에 대한 정보(주문 정보, 현재까지의
  생산량 등)를 표시한다. 표기 항목의 구체적 수준은 구현 시 결정한다.
- **대기 주문 확인**: 생산 큐(대기열)에 있는 대기 중인 생산 작업 목록을
  **FIFO** 순서로 표시한다.

## 6. 상태 전이 규칙

```
RESERVED --(승인, 재고 충분)--> CONFIRMED
RESERVED --(승인, 재고 부족)--> PRODUCING   (생산 라인에 자동 등록)
RESERVED --(거절)-------------> REJECTED    [최종]
PRODUCING --(생산 완료)-------> CONFIRMED
CONFIRMED --(출고 실행)-------> RELEASE      [최종]
```

## 7. 비기능 요구사항

- Python으로 구현하며, 콘솔(CLI) 환경에서 동작한다.
- 데이터는 3개 테이블(Sample, Order, ProductionQueue)로 관리하며,
  JSON 파일(`samples.json`, `orders.json`, `production_queue.json`)로
  영속화한다. 스키마 상세는
  [CLAUDE.md](./CLAUDE.md#데이터베이스-스키마) 참고.

